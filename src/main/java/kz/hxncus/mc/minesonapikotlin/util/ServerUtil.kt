package kz.hxncus.mc.minesonapikotlin.util

import org.bukkit.*
import org.bukkit.attribute.Attribute
import org.bukkit.entity.EntityType

/**
 * The Arts.
 */
val ARTS: Array<Art> = Art.entries.toTypedArray()

/**
 * The Attributes.
 */
val ATTRIBUTES: Array<Attribute> = Attribute.entries.toTypedArray()

/**
 * The Chat colors.
 */
val CHAT_COLORS: Array<ChatColor> = ChatColor.entries.toTypedArray()

/**
 * The Dye colors.
 */
val DYE_COLORS: Array<DyeColor> = DyeColor.entries.toTypedArray()

/**
 * The Effects.
 */
val EFFECTS: Array<Effect> = Effect.entries.toTypedArray()

/**
 * The Entity types.
 */
val ENTITY_TYPES: Array<EntityType> = EntityType.entries.toTypedArray()

/**
 * The Entity effects.
 */
val ENTITY_EFFECTS: Array<EntityEffect> = EntityEffect.entries.toTypedArray()

/**
 * The Instruments.
 */
val INSTRUMENTS: Array<Instrument> = Instrument.entries.toTypedArray()

/**
 * The Materials.
 */
val MATERIALS: Array<Material> = Material.entries.toTypedArray()

/**
 * The Particles.
 */
val PARTICLES: Array<Particle> = Particle.entries.toTypedArray()

/**
 * The Sounds.
 */
val SOUNDS: Array<Sound> = Sound.entries.toTypedArray()

/**
 * The Sound categories.
 */
val SOUND_CATEGORIES: Array<SoundCategory> = SoundCategory.entries.toTypedArray()

/**
 * The Statistics.
 */
val STATISTICS: Array<Statistic> = Statistic.entries.toTypedArray()

/**
 * The Tree types.
 */
val TREE_TYPES: Array<TreeType> = TreeType.entries.toTypedArray()

/**
 * The Min coordinate.
 */
const val MIN_COORDINATE: Int = -29999999

/**
 * The Max coordinate.
 */
const val MAX_COORDINATE: Int = 29999999
